/*import React from 'react'
import University from './University'
export default function UniversityList({univ}) {
    
    return (
    
    
    
    <div style={{marginTop:100,display:'flex',flexDirection:'row',justifyContent:'flex-start',alignItems:'center',flexWrap:'wrap'}}>

      {univ.map((u)=>(<University key={u._id} univ={u}></University>))}  
        </div>
    )
}
*/